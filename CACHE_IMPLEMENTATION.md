# Кеширование статистики Guardian NFT

## Обзор
Добавлено кеширование для `protectedAmount` и `scanCount` из Guardian NFT контракта для снижения нагрузки на блокчейн.

## Изменения

### 1. Новая функция кеширования
```python
async def get_guardian_stats_cached(token_id: int) -> tuple[int, int]:
```
- Кеширует данные на 1 час (3600 секунд)
- При отсутствии или устаревании кеша - запрашивает из контракта
- Автоматически сохраняет в БД

### 2. Обновление cmd_myguardian
- Заменены прямые вызовы контракта на `get_guardian_stats_cached()`
- Теперь использует кешированные данные при обычном вызове

### 3. Обновление cb_refresh_guardian
- По кнопке "Обновить данные" - принудительный запрос из контракта
- Сразу обновляет кеш новыми данными
- Гарантирует актуальность при ручном обновлении

### 4. Структура БД
Добавлено поле `guardian_stats_cache` в `_DB_DEFAULT`:
```python
"guardian_stats_cache": {},  # <-- кеш статистики Guardian NFT
```

### 5. Автоматическая очистка ботов ⭐
Обновлена функция `safe_send` для автоматического обнаружения и удаления ботов:
- При попытке отправки сообщения боту - автоматически удаляет его из:
  - `connected_wallets`
  - `user_guardians` 
  - `user_limits`
- Сохраняет изменения в БД
- Работает для всех рассылок (включая `broadcast_whale`)

## Преимущества
- ✅ Снижение нагрузки на RPC узлы
- ✅ Быстрый отклик при повторных запросах
- ✅ Принудительное обновление по кнопке
- ✅ Автоматическое обновление кеша при истечении TTL
- ✅ Автоматическая очистка БД от ботов-пользователей

## TTL кеша
- **Стандартный**: 1 час (3600 секунд)
- **Принудительное обновление**: по кнопке "🔄 Обновить данные"

## Формат кеша
```python
{
    "token_id": {
        "protected": int,
        "scans": int,
        "ts": float  # timestamp
    }
}
```
